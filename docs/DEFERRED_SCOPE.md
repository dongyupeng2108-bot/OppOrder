# 显式不排期 / 搁置范围

本文档对应总体计划中 **「不在 BTCQDD 执行机器人收口主线上推进」** 的事项，避免与 [`MODULE_MAP.md`](MODULE_MAP.md)、[`OWNER_BTCQDD_ONE_PAGER.md`](OWNER_BTCQDD_ONE_PAGER.md) 冲突。

## 搁置或低优先级（当前阶段）

| 项 | 说明 |
|----|------|
| **Opportunity Radar 产品扩展** | 53122 主站产品化、扫描/机会雷达新功能；仓库可能共存代码，**默认不改、不扩**。 |
| **通用策略平台** | 多策略可视化编排、页面驱动整套 `instances`、大重构 `server.mjs` 统一「平台化」。 |
| **策略生成（独立产品模块）** | 作为远景「策略生成 / 回测 / 执行」之一，**单独立项**，不在执行机器人 A/B/C 阶段并行推进。 |
| **回测（独立产品模块）** | 同上。 |
| **恢复已删除 UI 噪音字段** | 与 PROJECT_RULES、历史 UI 收口任务相反，除非 Owner 明确任务。 |

## 仍属执行机器人范围内（勿误解为搁置）

- 旧 **strategy 实例**与 **`instances/*.json`**：**保留**，作承载与兼容；**不**扩成管理平台。
- **Paper 模式**与版本测试：**主交付**，持续加固。

## 何时可重评「策略生成 / 回测」

当 [`PROJECT_MASTER_PLAN.md`](../rules/rules/PROJECT_MASTER_PLAN.md) **阶段 D** 被正式打开，且 Owner 批准独立里程碑与范围锁后，再单独更新本文档或拆出子计划。
