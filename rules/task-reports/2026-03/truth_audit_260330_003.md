# TraeTask_260330_003 验收摘要（当前窗口订单状态表展示口径）

## 结论

- 验收结论：**PASS**
- 结论：展示层收口完成（平仓价去重、类型列可读、父子单与生命周期清晰）

## 最小事实摘录

- 修复前“平仓价”重复：
  - `0.900 + tp:0.900` 同值重复显示。
- 修复后“平仓价”去重：
  - 同值仅显示主值 `0.900`，不再重复 `tp:0.900`。
- 修复前“类型列”坏样风险：
  - 原始 `TAKE_PROFIT` 文本直接入列，窄列下可读性差。
- 修复后“类型列”完整可读：
  - 统一显示 `进场单 / 止盈单 / 平仓单`。
- 修复后“父/子单 + 生命周期”更清楚：
  - 类型列子行显示 `父单/子单(...)`；
  - 状态列子行显示 `OPEN/FILLED/CANCELLED`。

## 不回退确认

- 未改执行主链
- 未改当前窗口过滤逻辑
- 未改头部概率快照逻辑
- 未改 signer/余额链
- 未改 PNL/today
