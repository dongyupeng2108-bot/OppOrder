# TraeTask_260330_003 实施记录（当前窗口订单状态表展示收口）

## 范围结论

- 本单仅做展示层调整：
  - `ui/js/strategy-editor.js`
  - `scripts/truth_audit_order_table_display_260330_003.mjs`（审计脚本）
- 未改执行主链、未改订单执行/撮合/撤单逻辑。

## 字段确认（改前先锁）

- 主值展示字段：
  - 价格列主值：`price`
  - 状态列主值：`status`
  - 平仓价列主值：`tp_price`（无 tp 时回退 `fill_price`）
- 子行补充字段：
  - 价格列子行：`fill:fill_price`
  - 平仓价列子行：`tp:*`（本单改为去重后按需显示）
- 生命周期字段：
  - `OPEN / FILLED / CANCELLED`

## 展示层收口

- 平仓价列：
  - 主值与 `tp` 子行重复时去重（不再重复 `tp:同值`）。
  - `tp=1` 统一显示 `等待结算 + 结算价:1.000`。
- 类型列：
  - `ENTRY -> 进场单`、`TAKE_PROFIT -> 止盈单`、`EXIT -> 平仓单`，避免长英文坏样。
  - 增加父子单子行：`父单` / `子单(parent_id前8位)`。
- 生命周期清晰化：
  - 状态列主值中文（挂单/成交/撤单）+ 子行英文码（OPEN/FILLED/CANCELLED）。

## 不回退确认

- 260329_007：当前窗口过滤逻辑未改。
- 260330_001：头部概率同 tick 快照逻辑未改。
