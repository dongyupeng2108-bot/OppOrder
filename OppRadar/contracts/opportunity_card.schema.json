{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Opportunity Card",
  "description": "Central contract for LLM-generated opportunity cards. Required by llm_gateway.mjs for schema validation.",
  "type": "object",
  "required": ["id", "title", "score", "run_id", "created_at"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique opportunity ID"
    },
    "title": {
      "type": "string",
      "description": "Human-readable opportunity title"
    },
    "score": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Opportunity score in [0,1]"
    },
    "run_id": {
      "type": "string",
      "description": "Associated run/scan ID"
    },
    "created_at": {
      "type": "string",
      "description": "ISO8601 creation timestamp"
    },
    "summary": {
      "type": "string",
      "description": "Optional LLM-generated summary"
    },
    "provider": {
      "type": "string",
      "description": "Provider that generated this card"
    },
    "opp_id": {
      "type": "string",
      "description": "Opportunity unique identifier for cross-snapshot tracking (M4)"
    },
    "snapshot_type": {
      "type": "string",
      "enum": ["draft", "deep"],
      "description": "Whether this card is a draft scan or a deep-dive snapshot (M4)"
    },
    "model_used": {
      "type": "string",
      "description": "LLM model identifier used to generate this card (M4)"
    },
    "p_range": {
      "type": "object",
      "description": "Probability range [low, high] ∈ [0,1]. Constraint: low ≤ high (JSON Schema draft-07 does not support cross-field validation; enforced at application layer).",
      "properties": {
        "low": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Lower bound of probability estimate"
        },
        "high": {
          "type": "number",
          "minimum": 0,
          "maximum": 1,
          "description": "Upper bound of probability estimate"
        }
      },
      "required": ["low", "high"]
    },
    "confidence": {
      "type": "string",
      "enum": ["High", "Med", "Low"],
      "description": "Information quality assessment for this opportunity (M4)"
    },
    "risk_triggers": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "maxItems": 3,
      "description": "Risk trigger labels (max 3) flagged during evaluation (M4)"
    },
    "veto": {
      "type": "object",
      "description": "Hard gate veto decision for this opportunity (M4)",
      "properties": {
        "decision": {
          "type": "string",
          "enum": ["ALLOW", "DEGRADE", "BLOCK"],
          "description": "Veto decision outcome"
        },
        "labels": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "maxItems": 3,
          "description": "Labels explaining the veto decision"
        },
        "rule_facts": {
          "type": "object",
          "description": "Key-value facts from the rule engine that triggered this decision"
        }
      }
    },
    "evidence_refs": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Evidence pointer references (URLs, file paths, or IDs) supporting this opportunity (M4)"
    },
    "p_mid": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Midpoint of p_range: (low + high) / 2 (M4 offset sorting)"
    },
    "dev_signed": {
      "type": "number",
      "minimum": -1,
      "maximum": 1,
      "description": "Signed deviation: p_mid - yes_price. Positive = model thinks market underprices (M4 offset sorting)"
    },
    "dev_abs": {
      "type": "number",
      "minimum": 0,
      "maximum": 1,
      "description": "Absolute deviation: |dev_signed|. Used for sorting by signal strength (M4 offset sorting)"
    },
    "dev_flag": {
      "type": "string",
      "enum": ["OVER", "UNDER", "FAIR"],
      "description": "Direction flag: OVER if dev_signed < -0.05 (market overprices), UNDER if > 0.05 (underprices), FAIR otherwise (M4 offset sorting)"
    },
    "mispricing_type": {
      "type": "string",
      "description": "LLM-identified mispricing category (e.g. 'sentiment_lag', 'info_gap', 'recency_bias') (M4 offset diagnostic)"
    },
    "why_1_liner": {
      "type": "string",
      "maxLength": 120,
      "description": "One-liner explanation of why the model disagrees with market price (M4 offset diagnostic)"
    },
    "what_to_check": {
      "type": "string",
      "maxLength": 200,
      "description": "Suggested verification step or data source to validate the edge (M4 offset diagnostic)"
    },
    "model_role": {
      "type": "string",
      "enum": ["scan", "core"],
      "description": "Model role: 'scan' for draft_generator pass, 'core' for gemini deep-dive pass (M4 model source)"
    },
    "latency_ms": {
      "type": "number",
      "minimum": 0,
      "description": "LLM call round-trip latency in milliseconds (M4 model source)"
    }
  }
}
